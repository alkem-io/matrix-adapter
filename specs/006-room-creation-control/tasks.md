# Tasks: Room Creation Control

**Input**: Design documents from `/specs/006-room-creation-control/`
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅, contracts/ ✅

**Tests**: Unit tests included as per Constitution §6 (Pragmatic Testing with SDK Boundaries)

**Organization**: Tasks grouped by user story priority (P1 first) for independent implementation

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

---

## Phase 1: Setup (DTOs & Topic Constants)

**Purpose**: Foundation for DM request notification - enables TypeScript lib generation

- [X] T001 Create DM DTOs in pkg/dto/dm.go (DMRequestedEvent, DMWebhookPayload)
- [X] T002 Add DM topic constant to pkg/dto/commands.go (TopicRoomDMRequested)
- [X] T003 Register DMRequestedEvent in OutgoingEventRegistry in pkg/dto/commands.go
- [X] T004 Run `make generate` to update lib/src/dto/generated.ts with new DM types

**Checkpoint**: DTOs defined, TypeScript definitions generated ✅

**Note**: No `CreateDMRoomCommand` needed - Server uses existing `communication.room.create` with `type: "direct"`

---

## Phase 2: Foundational (Shared Infrastructure)

**Purpose**: Verify existing infrastructure supports DM flow

> **Note**: After code review, `QueuePort.Publish()` already exists and `IDMapper.AlkemioActorID()` already handles Matrix→Alkemio ID conversion. No new infrastructure needed.

- [x] T005 ~~Add QueuePublisher interface~~ - SKIPPED: `QueuePort.Publish()` already exists in `internal/core/ports/queue.go`
- [x] T006 ~~Implement event publishing~~ - SKIPPED: `WatermillAdapter.Publish()` already works (used by `EventService`)
- [x] T007 ~~Add ID extraction helper~~ - SKIPPED: `IDMapper.AlkemioActorID()` exists in `internal/core/domain/idmapper.go`

**Checkpoint**: Existing infrastructure verified - proceed to user stories

---

## Phase 3: User Story 1 - Block Unauthorized Community Room Creation (Priority: P1) 🎯 MVP

**Goal**: Ghost users cannot create community rooms via Element - blocked at Synapse level

**Independent Test**: Attempt to create a named room in Element as ghost user → M_FORBIDDEN error

> **VERIFIED**: Synapse module `synapse-modules/alkemio_room_control.py` already exists and fully implements US1. Module README exists. No adapter code needed.

- [x] T008-T010 ~~Synapse module tasks~~ - SKIPPED: Module exists at `synapse-modules/alkemio_room_control.py` with documentation

**Checkpoint**: US1 complete - existing Synapse module handles blocking

---

## Phase 4: User Story 2 - DM Room Request Flow (Priority: P1) 🎯 MVP

**Goal**: Users can initiate DMs from Element; webhook notifies adapter; event published to RabbitMQ

**Independent Test**: Webhook POST to `/_matrix/app/alkemio/dm-request` → 200 OK + event published

### Implementation for User Story 2

- [X] T011 [US2] Create DM service in `internal/core/service/dm_service.go` with `PublishDMRequest()` method + unit tests
- [X] T012 [US2] Create DM webhook handler in `internal/infrastructure/http/dm_webhook.go` with HS token validation + unit tests
- [X] T013 [US2] Wire webhook handler into HTTP server in `internal/app/app.go`

**Checkpoint**: US2 complete - webhook receives DM requests, validates auth, publishes events ✅

---

## Phase 5: User Story 3 - Room Creation (EXISTING HANDLER)

**Goal**: Server creates DM room using standard `communication.room.create` with `type: "direct"`

**Independent Test**: Send CreateRoomRequest with `type: "direct"` and two initial_members → DM room created

> **NO NEW CODE NEEDED**: Existing `HandleCreateRoom` in `internal/infrastructure/queue/handler_room.go` already handles `type: "direct"` rooms. Server just needs to:
> 1. Subscribe to `communication.room.dm.requested` events
> 2. Send `CreateRoomRequest` with `type: "direct"` and both actors in `initial_members`

- [x] T014-T015 ~~DM handler tasks~~ - SKIPPED: Existing `HandleCreateRoom` handles DM rooms

**Checkpoint**: US3 complete - DM rooms created via existing room creation handler

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Final integration and documentation

- [X] T016 Update README.md with DM flow documentation (includes Synapse module setup notes)

> **Note**: TypeScript exports auto-generated by `make generate`. Quickstart validation and security verification are PR review checklist items, not discrete tasks.

**Checkpoint**: Documentation complete ✅

---

## Dependencies & Execution Order

### Phase Dependencies

```
Phase 1: Setup (T001-T004)
    ↓
Phase 2: Foundational - SKIPPED (existing code sufficient)
    ↓
Phase 3: US1 - SKIPPED (Synapse module exists)
    ↓
Phase 4: US2 (T011-T013) - webhook + event publishing
    ↓
Phase 5: US3 - SKIPPED (existing HandleCreateRoom handles DM rooms)
    ↓
Phase 6: Polish (T016)
```

### User Story Dependencies

- **US1 (Block Room Creation)**: ✅ DONE - Synapse module exists
- **US2 (DM Request Flow)**: Depends on Phase 1 (DTOs) - uses existing `QueuePort.Publish()`
- **US3 (Room Creation)**: ✅ DONE - Existing `HandleCreateRoom` with `type: "direct"` handles this

### Parallel Opportunities

**Setup Phase (T001-T004)**:
- T001, T002, T003 can run in parallel
- T004 depends on T001-T003 completion

**User Story Phases**:
- US2 and US3 can proceed in parallel after Setup
- Tests accompany implementation (no separate TDD tasks)

---

## Implementation Strategy

### MVP First (All P1 Stories)

All three user stories are P1 priority and together form the MVP:

1. Complete Phase 1: Setup (DTOs & Topics)
2. Complete Phase 2: Foundational (Event Publishing)
3. Complete Phase 3: US1 (Synapse module verification)
4. Complete Phase 4: US2 (Webhook → Event)
5. Complete Phase 5: US3 (Command → Room)
6. **VALIDATE**: Test full DM flow end-to-end
7. Complete Phase 6: Polish

### Exit Criteria Mapping

| Success Criteria | Tasks |
|-----------------|-------|
| SC-001: Block unauthorized room creation | ✅ Synapse module exists |
| SC-002: DM flow < 5 seconds | T011-T013 (US2) + existing HandleCreateRoom |
| SC-003: Webhook 401 for invalid tokens | T012 (webhook handler) |
| SC-004: HS token never in logs | PR review checklist |

---

## Notes

- [Story] label maps task to specific user story (US1, US2, US3)
- Constitution §1: All Matrix SDK calls in `internal/infrastructure/matrix` only
- Constitution §6: Unit tests mock ports; existing `RoomService.CreateRoomWithAlkemioID()` used for DM creation
- Tests accompany implementation in same task (no separate TDD tasks)
- Commit after each task or logical group

## PR Review Checklist (not discrete tasks)

- [X] `make generate` produces valid TypeScript types
- [X] `make lint` passes
- [ ] HS token never appears in log output (SC-004)
- [ ] Quickstart.md steps work end-to-end
